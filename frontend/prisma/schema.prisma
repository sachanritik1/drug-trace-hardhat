generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model Register{
  public_address String
  role String 
  @@id([public_address, role])
}